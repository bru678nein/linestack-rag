"""Responsibility: turning a company domain into documents, computed signals, and
a classified record of everything that failed.

The crawler currently lives at the repository root as `ingest.py` and is not
imported from here. It moves into this package once it has fixture-based unit
tests; see ADR-0010 and docs/open-questions.md section 3.4.
"""
